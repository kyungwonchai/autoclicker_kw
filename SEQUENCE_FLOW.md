# ⚔️ 던파모바일 오토클리커 캐릭키움 모드 상태 시퀀스 가이드

본 문서는 캐릭키움 모드(🌱)에서 화면 상태를 감지하고 실행하는 전체 흐름 및 시퀀스 규칙을 정리한 문서입니다. 필요 시 수치를 직접 보정하거나 규칙을 수정할 수 있습니다.

---

## 📌 1. 상태 감지 우선순위 (실시간 모니터: 0.3초 주기)

화면을 캡처한 뒤 아래 **우선순위(0순위 -> 7순위)** 순서로 일치하는 첫 번째 상태를 실행합니다.

| 순위 | 상태 명칭 | 감지 조건 및 특징 | 실행 동작 (터치 / 제스처) |
| :---: | :--- | :--- | :--- |
| **0순위** | **가이드 손가락 / 동그라미 (👆)** | 화면 내 연노랑 검지 손가락 + 노란색/주황색 원형 파동 링(Circle Ring) 애니메이션 | 손가락 끝 지점(`tipX`, `tipY`)을 즉시 탭 (맵 선택, 튜토리얼, 가이드 어디든 최우선 돌파) |
| **1순위** | **아이템 줍기 (🖐️ 손모양)** | 던전 전투 중 우하단 공격 버튼이 손모양(노란 손+흑갈색 배경)으로 변경 | 우하단 줍기 버튼(`x=82.5%`, `y=82.5%`) 2회 연속 탭 |
| **2순위** | **대화 스킵 (✕)** | 하단에 어두운 대화창 존재 + 우상단 건너뛰기 ✕ 흰색 글자 감지 | 우상단 건너뛰기 버튼(`x=92%`, `y=5.5%`) 탭 |
| **3순위** | **완료/확인 팝업** | 화면 중앙에 황금색 확인/이동 버튼 감지 | 중앙 확인 버튼(`x=56%`, `y=63%`) 탭 |
| **4순위** | **NPC 퀘스트 선택 / 보고** | 상단 퀘스트 창 타이틀 + 최상단 에픽 퀘스트 우측 주황색 [보고]/[수락] 버튼 감지 | 에픽 보고 버튼(`x=65.5%`, `y=35.5%`) 탭 |
| **5순위** | **던전 입장 / 전투시작** | 우하단 황금색/주황색 [입장] 또는 [전투시작] 버튼 감지 (`y: 80% ~ 88%`) | [전투시작] 버튼(`x=78.5%`, `y=84.0%`) 탭 |
| **6순위** | **던전 클리어 메뉴 (보스 클리어)** | 보스 처치 후 우측에 [던전 재도전하기] 메뉴 감지 (`x: 78% ~ 95%`) | **[보스 아이템 줍기 룰]** 아래 2번 항목 참조 |
| **7순위** | **마을 우상단 에픽 퀘스트** | 마을 상태 + 우상단 퀘스트 패널의 [에픽] 황금색 텍스트/테두리 아우라 감지 | 우상단 퀘스트(`x=82.6%`, `y=20.2%`) 탭 (이동 중 재클릭 방지: 쿨다운 12초) |

---

## 🔁 2. 보스 클리어 후 아이템 줍기 시퀀스 (3초 무반응 룰)

보스를 처치한 후 바닥에 퀘스트 아이템 등이 남아있으면 "아차 아이템을 가지고 가야지" 말풍선이 뜨며 진행이 막힙니다. 이를 해결하는 3초 룰 시퀀스입니다:

```
[보스 처치 / 클리어 메뉴 감지]
       │
       ▼
1. 우상단 다음 퀘스트(x=85%, y=14%) 1회 탭
       │
       ├─► (정상) 다음 던전 진입 또는 마을 로딩 전환 시 -> 일반 루프로 복귀
       │
       ▼ (화면 안 바뀌고 2.2초 이상 멈춤 / 바닥에 아이템 존재)
2. 타겟 위치(🎯)를 5초간 길게 꾹 누르기 (Hold 5000ms)
       │ -> 캐릭터가 주변 바닥 아이템을 전부 자동으로 흡수/습득
       ▼
3. 5초 홀드 직후 우상단 퀘스트(x=85%, y=14%) 재탭
       │
       ▼
4. 던전 선택창 또는 마을로 정상 진행 완료!
```

---

## 🏃 3. 마을 퀘스트 이동 중 재클릭 방지 (이동 안정화)

* **문제 현상**: 캐릭터가 퀘스트를 누르고 NPC나 던전 입구로 달리는 중에도 우상단 퀘스트 박스는 계속 떠 있으므로, 4~5초마다 다시 눌러서 캐릭터 이동이 끊기는 문제.
* **해결 규칙**: 마을에서 우상단 퀘스트를 탭한 후 **12초 동안 재클릭 쿨다운**을 유지합니다.
* **예외 처리**: 이동 도중 NPC 대화창(`isSkipDialog`), 확인 팝업(`isConfirmPopup`), 던전 선택창(`isBattleStart`)이 나타나면 12초 쿨다운과 무관하게 상위 순위로 즉시 처리됩니다.

---

## ⚙️ 4. 주요 좌표 및 감지 설정값 (코드 위치: AutoClickAccessibilityService.kt)

| 항목 | X 비율 / 좌표 | Y 비율 / 좌표 | 파일 내 함수 / 변수 |
| :--- | :--- | :--- | :--- |
| **전투시작 버튼** | `screenW * 0.785f` | `screenH * 0.840f` | `checkBattleStartInBitmap` |
| **우상단 최상단 퀘스트** | `screenW * 0.826f` | `screenH * 0.202f` | `checkQuestAuraInBitmap` |
| **클리어 메뉴 퀘스트** | `screenW * 0.850f` | `screenH * 0.140f` | `checkRetryButtonInBitmap` 분기 |
| **대화 건너뛰기 ✕** | `screenW * 0.920f` | `screenH * 0.055f` | `checkSkipDialogInBitmap` |
| **중앙 확인/이동** | `screenW * 0.560f` | `screenH * 0.630f` | `checkConfirmPopupInBitmap` |
| **에픽 보고/수락** | `screenW * 0.655f` | `screenH * 0.355f` | `checkQuestSelectPopupInBitmap` |
| **힐(손모양)** | `screenW * 0.472f` | `screenH * 0.910f` | `checkAndHandleLowHp` (HP 50% 이하 시) |
| **포션(물약)** | `screenW * 0.523f` | `screenH * 0.910f` | `checkAndHandleLowHp` (HP 50% 이하 시) |
