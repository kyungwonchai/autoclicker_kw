# 📋 오토클리커 (던파모바일 캐릭키움) 버그 발생 이력 및 해결 기록

본 문서는 개발 과정에서 발생했던 모든 버그와 원인(실수), 그리고 각 항목의 근본적인 해결 조치 내역을 상세히 기록한 문서입니다.

---

## 1. 전투 중 "누를락말락 지랄병/발작" 버그
* **증상**: 던전 전투 중 기본 공격 버튼을 꾹 누르고 있어야 하는데, 0.4초마다 손을 뗐다 다시 누르며 캐릭터가 평타 1타만 치고 멈추는 발작 증상 발생.
* **원인 (실수)**:
  - 제스처 중간 취소 및 반응성을 높이겠다고 `holdDurationMs`(5000ms)를 400ms 단위의 청크 루프(`for (chunk in 1..12)`)로 쪼개어 디스패치함.
  - Android AccessibilityService는 각 청크를 별개의 터치 이벤트(Down -> Up)로 처리하여 400ms마다 버튼을 연타하는 형태가 됨.
* **해결 조치**:
  - 400ms 청크 분할 루프를 전면 폐기하고, 단일 5000ms 연속 제스처(`GestureDescription.StrokeDescription(path, 0L, holdDurationMs)`)로 원복하여 5초간 끊김 없는 순수 홀드 공격 복원.

---

## 2. 던전 내부에서 멈춤/멍때림 버그
* **증상**: 던전 안에 진입해 미니맵이 떠 있는데도 공격을 전혀 하지 않고 캐릭터가 멈춰 서 있는 문제.
* **원인 (실수)**:
  - 화면 좌상단의 캐릭터 레벨/닉네임 흰색 텍스트가 던전 선택창의 [던전 선택] 텍스트 판정 조건(`titlePts > 150`)을 잘못 만족함.
  - 이로 인해 `isInDungeonState`가 `false`로 바뀌었고, `executionJob`에서 `if (!isInDungeonState) continue`로 인해 공격 루프가 영구 스킵됨.
* **해결 조치**:
  - 미니맵 검은 영역 비율 검사(`rawMiniMapDarkRatio > 0.35f`)를 통해 미니맵이 존재하면 무조건 던전 내부로 판정하고, 던전 선택창 판정에서 즉시 `false`를 반환하도록 완벽 분리.

---

## 3. 마을 및 UI에서 공격 홀드(5초)가 실행되어 멈추는 버그
* **증상**: 마을에서 퀘스트를 누르거나 이동해야 하는데, 공격 버튼 위치를 5초간 꾹 누르고 있어 화면 진행이 안 됨.
* **원인 (실수)**:
  - 2번 버그를 해결하려던 중 `executionJob`에서 `if (!isInDungeonState) continue`를 통째로 주석 처리함.
  - 그 결과 마을, 던전 선택창, 대화창 어디서든 5초 공격 홀드가 무조건 실행되었고, 제스처 뮤텍스(`gestureMutex`)를 5초간 독점하여 백그라운드 모니터의 화면 터치들이 모두 대기/블로킹됨.
* **해결 조치**:
  - `executionJob`에 `if (!isInDungeonState.get() || growthState == GrowthState.DIALOG_PROGRESS) { delay(100L); continue; }` 조건을 확실히 적용하여 **오직 던전 전투 중일 때만 공격 홀드가 돌도록 제한**.
  - `gestureMutex`의 5초간 대기 락을 제거하여 모니터 탭이 즉각 디스패치되도록 개선.

---

## 4. 우상단 [에픽]을 눌러야 하는데 우하단 쓸데없는 버튼을 연타하는 버그
* **증상**: 우상단에 [에픽] 퀘스트가 떠서 눌러야 하는 상황인데, 우하단 엉뚱한 위치(`2826, 1278`)를 계속 탭함.
* **원인 (실수)**:
  - 1) `findBattleStartButtonInBitmap`의 황금색 픽셀 임계치가 너무 낮아(`goldPixels > 30`), 마을 우하단 메뉴 아이콘(가방/스킬 등)의 노란 픽셀 30개만으로 [입장] 버튼으로 오탐됨.
  - 2) `hasEpicQuest` 감지 블록 내부에서 `isDungeonSelect`와 `battleStartCoord`를 먼저 체크하도록 잘못 작성되어, [에픽]이 화면에 버젓이 떠 있음에도 오탐된 우하단 [입장] 좌표를 우선 터치함.
* **해결 조치**:
  - 1) [입장] 버튼 검출 기준을 실제 큰 버튼 크기에 맞게 `goldPixels > 250 && ratio > 0.08f`로 대폭 상향하고 마을 레이더 화면(`checkTownScreenInBitmap`)이면 던전 선택창에서 배제.
  - 2) 우선순위 파이프라인에서 7순위에 **우상단 [에픽] 퀘스트(86.5%, 18.3%)**를 독립 배치하고, 에픽이 감지되면 즉시 탭 후 `return`하여 하위 던전 선택/입장 버튼으로 절대 빠지지 않도록 원천 차단.

---

## 5. 플로팅 오버레이 슬롯 버튼 정리 요청 누락
* **요청**: "5, 10, 15 중에 5만 남겨라"
* **해결 조치**:
  - `FloatingOverlayManager.kt`의 `slots` 초기화 시 오직 5초 슬롯(`it.name == "5" || it.holdDurationMs == 5000L`) 1개만 등록하도록 필터링 완료.

---

---

## 7. 퀘스트 클릭 후 "이동 중" 상태에서 연타로 이동이 끊기는 버그
* **증상**: 마을에서 퀘스트를 누른 후 캐릭터가 목적지로 뛰어가는("이동 중") 도중에도 퀘스트를 1~2초마다 계속 다시 클릭하여 캐릭터가 제자리에서 멈칫거리며 이동이 취소/재시작되는 문제.
* **원인 (실수)**:
  - 퀘스트 클릭 후 쿨다운이 1.2초로 너무 짧았고, 화면 퀘스트 영역 하단에 떠 있는 **"이동 중"** 텍스트를 감지하여 대기하는 로직이 누락되어 있었음.
* **해결 조치**:
  - `checkMovingInTownInBitmap` 함수 복원 및 고도화: 퀘스트 영역(`x: 82% ~ 94%, y: 19% ~ 25%`)에 "이동 중" 텍스트(흰색/노란색)가 감지되면 캐릭터가 목적지에 도착할 때까지 **퀘스트 재클릭을 100% 차단하고 대기**.
  - 퀘스트 1회 클릭 시 기본 쿨다운을 6초로 대폭 연장하고 `TOWN_MOVING` 상태로 전환하여 안전한 이동 보장.

---

## 8. 던전 선택 화면에서 반짝이는 지도(맵 카드)를 클릭하지 못하던 버그
* **증상**: 던전 선택창에 진입하여 반짝이는 사각형 지도 맵 카드를 선택하고 [입장]을 눌러야 하는데, 지도를 전혀 클릭하지 못하는 문제.
* **원인 (실수)**:
  - 던전 선택 화면에서도 우상단 [에픽] 퀘스트 배너가 화면 우측에 계속 표시되어 있음.
  - 파이프라인에서 [에픽] 퀘스트가 6순위로 던전 선택창(8순위)보다 위에 배치되어 있어, 던전 선택 화면임에도 맵 카드 대신 에픽 퀘스트만 계속 탭하거나 리턴하여 맵 카드 감지 블록에 도달하지 못함.
* **해결 조치**:
  - 던전 선택 화면 판정(`checkDungeonSelectScreenInBitmap`)을 **마을 에픽 퀘스트보다 상위(6순위)**로 전진 배치.
  - 던전 선택창에서는 **반짝이는 사각형 퀘스트 맵 카드(`findQuestMapCardInBitmap`)를 최우선 즉시 클릭**하고, 카드 선택 후 활성화되는 **[입장/전투시작] 버튼을 즉각 클릭**하도록 수정 완료.

---

## 9. 최종 확정 우선순위 파이프라인

| 순위 | 대상 | 터치 좌표 / 동작 | 비고 |
| :---: | :--- | :--- | :--- |
| **0순위** | **장비 장착 팝업** | `(x=90.5%, y=68.0%)` 탭 | 획득 즉시 착용 |
| **1순위** | **대화 스킵 (✕)** | `(x=91.5%, y=6.0%)` 탭 | 하단 대화창 존재 시에만 스킵 |
| **2순위** | **완료/확인/이동 팝업** | `(x=56.0%, y=63.0%)` 탭 | 중앙 모달 확인 |
| **3순위** | **NPC 퀘스트 수락/보고** | `(x=65.5%, y=35.5%)` 탭 | 최상단 에픽 수락/보고 |
| **4순위** | **던전 클리어 메뉴** | `(x=86.5%, y=18.3%)` 탭 | 보스 처치 후 다음 퀘스트 진행 |
| **5순위** | **던전 내부 전투** | 줍기손 / 초입 3회 탭 / 5초 홀드 | 화면 헛클릭 일체 차단 |
| **6순위** | **던전 선택 화면** | **반짝이는 맵 카드 클릭** -> [입장] 클릭 | **마을 퀘스트보다 최우선 선택** |
| **7순위** | **마을 우상단 [에픽] 퀘스트** | `(x=86.5%, y=18.3%)` 탭 | 마을 길찾기 이동 (3.5초 쿨다운) |
| **8순위** | **가이드 손가락/링 (👆)** | `(tipX, tipY)` 탭 | 튜토리얼 돌파 |

---

## 10. 던전 진입 후 공격하지 않고 멍때리며 맞고만 있던 버그
* **증상**: 던전에 들어왔는데 공격을 전혀 하지 않고 처맞고만 있으며, 바닥에 엉뚱한 터치가 튀는 문제.
* **원인 (실수)**:
  - 1) `checkDungeonSelectScreenInBitmap`에서 좌상단 흰색 픽셀 판정(`titlePts > 150`)을 사용했는데, 실제 던전 전투 중 좌상단에 표시되는 캐릭터 닉네임/레벨/계급 흰색 텍스트(1340픽셀)가 이를 만족해 **전투 중에도 던전 선택창으로 오탐**됨.
  - 2) 이로 인해 `isInDungeonState = false`가 되어 `executionJob`의 공격 꾹 누르기(5초 홀드)가 완전히 정지됨.
  - 3) 또한 던전 선택창으로 오탐된 상태에서 `findQuestMapCardInBitmap`이 실행되어, 전투 중 발생하는 몬스터 피격/스킬 이펙트 점멸을 맵 카드로 착각하고 바닥을 난타함.
  - 4) `startSlotExecution` 시작 시 `isInDungeonState` 초기값이 `false`로 되어 있어 5번을 눌렀을 때 즉시 홀드로 진입하지 못함.
* **해결 조치**:
  - 1) **전투 컨트롤 판정기(`checkCombatControlsInBitmap`) 신설**: 화면에 황금색 검 공격 버튼(`atkPts > 80`) 또는 조이스틱 링(`joyPts > 60`)이 감지되면 **무조건 던전 전투 중(`isCombat = true`)**으로 즉시 판정.
  - 2) 던전 선택창 판정에서 `checkCombatControlsInBitmap` 감지 시 즉시 `false` 반환하여 전투 중 던전 선택창 오탐 원천 차단.
  - 3) `startSlotExecution` 시작 시 `isInDungeonState` 초기값을 `true`로 설정하여 사용자가 5번 버튼을 누르자마자 즉각 5초 공격 홀드 시작.
  - 4) `findBattleStartButtonInBitmap` 및 `findQuestMapCardInBitmap`에도 전투 컨트롤 감지 시 즉시 차단하는 이중 안전장치 적용.

---

## 11. 던전 진입 5회 클릭 및 전투 중 연속 꾹 누르기(홀드) 구조 정립
* **증상**: 던전 첫 진입 시에만 5회 클릭 후 공격 홀드로 넘어가야 하는데, 홀드가 끝날 때마다 임의로 추가했던 스킬 시전 및 조이스틱 스와이프가 튀어나와 전투 흐름이 끊기는 문제.
* **원인 (실수)**:
  - 던전 전투 중 매 홀드마다 임의로 스킬 클릭과 조이스틱 방향 스와이프(`swipeSingle`)를 호출하도록 작성되어 있어, 사용자가 원하는 "던전 초입에만 5회 클릭 후, 그 뒤로는 텀을 두고 꾹 누르기(5초 홀드 공격)만 이어서 계속 반복"하는 단순하고 확실한 동작을 방해함.
* **해결 조치**:
  - 1) `hasDoneDungeonInitialClicks` 플래그 도입: 던전 처음 진입 시 딱 1회만 타겟 공격 버튼 5회 클릭 실행.
  - 2) 던전 내부 전투 루프에서는 임의의 조이스틱 스와이프 및 스킬 난사를 전면 제거하고, **5초 꾹 누르기(Hold) -> 150ms 텀(간격) -> 5초 꾹 누르기(Hold)**를 끊김 없이 연속으로 정직하게 반복하도록 단순화.
  - 3) 던전 클리어/던전 선택/마을 이동 시 `hasDoneDungeonInitialClicks`를 리셋하여 다음 던전 진입 시 정상적으로 5회 클릭 후 홀드하도록 완벽 연동.
