# 📋 오토클리커 (던파모바일 캐릭키움) 버그 발생 이력 및 해결 기록

본 문서는 개발 과정에서 발생했던 모든 버그와 원인(실수), 그리고 각 항목의 근본적인 해결 조치 내역을 상세히 기록한 문서입니다.

---

## 1. 전투 중 "누를락말락 지랄병/발작" 버그
* **증상**: 던전 전투 중 기본 공격 버튼을 꾹 누르고 있어야 하는데, 0.4초마다 손을 뗐다 다시 누르며 캐릭터가 평타 1타만 치고 멈추는 발작 증상 발생.
* **원인 (실수)**:
  - 제스처 중간 취소 및 반응성을 높이겠다고 `holdDurationMs`(5000ms)를 400ms 단위의 청크 루프(`for (chunk in 1..12)`)로 쪼개어 디스패치함.
  - Android AccessibilityService는 각 청크를 별개의 터치 이벤트(Down -> Up)로 처리하여 400ms마다 버튼을 연타하는 형태가 됨.
* **해결 조치**:
  - 400ms 청크 분할 루프를 전면 폐기하고, 단일 5000ms 연속 제스처(`GestureDescription.StrokeDescription(path, 0L, holdDurationMs)`)로 원복하여 5초간 끊김 없는 순수 홀드 공격 복원.

---

## 2. 던전 내부에서 멈춤/멍때림 버그
* **증상**: 던전 안에 진입해 미니맵이 떠 있는데도 공격을 전혀 하지 않고 캐릭터가 멈춰 서 있는 문제.
* **원인 (실수)**:
  - 화면 좌상단의 캐릭터 레벨/닉네임 흰색 텍스트가 던전 선택창의 [던전 선택] 텍스트 판정 조건(`titlePts > 150`)을 잘못 만족함.
  - 이로 인해 `isInDungeonState`가 `false`로 바뀌었고, `executionJob`에서 `if (!isInDungeonState) continue`로 인해 공격 루프가 영구 스킵됨.
* **해결 조치**:
  - 미니맵 검은 영역 비율 검사(`rawMiniMapDarkRatio > 0.35f`)를 통해 미니맵이 존재하면 무조건 던전 내부로 판정하고, 던전 선택창 판정에서 즉시 `false`를 반환하도록 완벽 분리.

---

## 3. 마을 및 UI에서 공격 홀드(5초)가 실행되어 멈추는 버그
* **증상**: 마을에서 퀘스트를 누르거나 이동해야 하는데, 공격 버튼 위치를 5초간 꾹 누르고 있어 화면 진행이 안 됨.
* **원인 (실수)**:
  - 2번 버그를 해결하려던 중 `executionJob`에서 `if (!isInDungeonState) continue`를 통째로 주석 처리함.
  - 그 결과 마을, 던전 선택창, 대화창 어디서든 5초 공격 홀드가 무조건 실행되었고, 제스처 뮤텍스(`gestureMutex`)를 5초간 독점하여 백그라운드 모니터의 화면 터치들이 모두 대기/블로킹됨.
* **해결 조치**:
  - `executionJob`에 `if (!isInDungeonState.get() || growthState == GrowthState.DIALOG_PROGRESS) { delay(100L); continue; }` 조건을 확실히 적용하여 **오직 던전 전투 중일 때만 공격 홀드가 돌도록 제한**.
  - `gestureMutex`의 5초간 대기 락을 제거하여 모니터 탭이 즉각 디스패치되도록 개선.

---

## 4. 우상단 [에픽]을 눌러야 하는데 우하단 쓸데없는 버튼을 연타하는 버그
* **증상**: 우상단에 [에픽] 퀘스트가 떠서 눌러야 하는 상황인데, 우하단 엉뚱한 위치(`2826, 1278`)를 계속 탭함.
* **원인 (실수)**:
  - 1) `findBattleStartButtonInBitmap`의 황금색 픽셀 임계치가 너무 낮아(`goldPixels > 30`), 마을 우하단 메뉴 아이콘(가방/스킬 등)의 노란 픽셀 30개만으로 [입장] 버튼으로 오탐됨.
  - 2) `hasEpicQuest` 감지 블록 내부에서 `isDungeonSelect`와 `battleStartCoord`를 먼저 체크하도록 잘못 작성되어, [에픽]이 화면에 버젓이 떠 있음에도 오탐된 우하단 [입장] 좌표를 우선 터치함.
* **해결 조치**:
  - 1) [입장] 버튼 검출 기준을 실제 큰 버튼 크기에 맞게 `goldPixels > 250 && ratio > 0.08f`로 대폭 상향하고 마을 레이더 화면(`checkTownScreenInBitmap`)이면 던전 선택창에서 배제.
  - 2) 우선순위 파이프라인에서 7순위에 **우상단 [에픽] 퀘스트(86.5%, 18.3%)**를 독립 배치하고, 에픽이 감지되면 즉시 탭 후 `return`하여 하위 던전 선택/입장 버튼으로 절대 빠지지 않도록 원천 차단.

---

## 5. 플로팅 오버레이 슬롯 버튼 정리 요청 누락
* **요청**: "5, 10, 15 중에 5만 남겨라"
* **해결 조치**:
  - `FloatingOverlayManager.kt`의 `slots` 초기화 시 오직 5초 슬롯(`it.name == "5" || it.holdDurationMs == 5000L`) 1개만 등록하도록 필터링 완료.

---

---

## 7. 퀘스트 클릭 후 "이동 중" 상태에서 연타로 이동이 끊기는 버그
* **증상**: 마을에서 퀘스트를 누른 후 캐릭터가 목적지로 뛰어가는("이동 중") 도중에도 퀘스트를 1~2초마다 계속 다시 클릭하여 캐릭터가 제자리에서 멈칫거리며 이동이 취소/재시작되는 문제.
* **원인 (실수)**:
  - 퀘스트 클릭 후 쿨다운이 1.2초로 너무 짧았고, 화면 퀘스트 영역 하단에 떠 있는 **"이동 중"** 텍스트를 감지하여 대기하는 로직이 누락되어 있었음.
* **해결 조치**:
  - `checkMovingInTownInBitmap` 함수 복원 및 고도화: 퀘스트 영역(`x: 82% ~ 94%, y: 19% ~ 25%`)에 "이동 중" 텍스트(흰색/노란색)가 감지되면 캐릭터가 목적지에 도착할 때까지 **퀘스트 재클릭을 100% 차단하고 대기**.
  - 퀘스트 1회 클릭 시 기본 쿨다운을 6초로 대폭 연장하고 `TOWN_MOVING` 상태로 전환하여 안전한 이동 보장.

---

## 8. 던전 선택 화면에서 반짝이는 지도(맵 카드)를 클릭하지 못하던 버그
* **증상**: 던전 선택창에 진입하여 반짝이는 사각형 지도 맵 카드를 선택하고 [입장]을 눌러야 하는데, 지도를 전혀 클릭하지 못하는 문제.
* **원인 (실수)**:
  - 던전 선택 화면에서도 우상단 [에픽] 퀘스트 배너가 화면 우측에 계속 표시되어 있음.
  - 파이프라인에서 [에픽] 퀘스트가 6순위로 던전 선택창(8순위)보다 위에 배치되어 있어, 던전 선택 화면임에도 맵 카드 대신 에픽 퀘스트만 계속 탭하거나 리턴하여 맵 카드 감지 블록에 도달하지 못함.
* **해결 조치**:
  - 던전 선택 화면 판정(`checkDungeonSelectScreenInBitmap`)을 **마을 에픽 퀘스트보다 상위(6순위)**로 전진 배치.
  - 던전 선택창에서는 **반짝이는 사각형 퀘스트 맵 카드(`findQuestMapCardInBitmap`)를 최우선 즉시 클릭**하고, 카드 선택 후 활성화되는 **[입장/전투시작] 버튼을 즉각 클릭**하도록 수정 완료.

---

## 9. 최종 확정 우선순위 파이프라인

| 순위 | 대상 | 터치 좌표 / 동작 | 비고 |
| :---: | :--- | :--- | :--- |
| **0순위** | **장비 장착 팝업** | `(x=90.5%, y=68.0%)` 탭 | 획득 즉시 착용 |
| **1순위** | **대화 스킵 (✕)** | `(x=91.5%, y=6.0%)` 탭 | 하단 대화창 존재 시에만 스킵 |
| **2순위** | **완료/확인/이동 팝업** | `(x=56.0%, y=63.0%)` 탭 | 중앙 모달 확인 |
| **3순위** | **NPC 퀘스트 수락/보고** | `(x=65.5%, y=35.5%)` 탭 | 최상단 에픽 수락/보고 |
| **4순위** | **던전 클리어 메뉴** | `(x=86.5%, y=18.3%)` 탭 | 보스 처치 후 다음 퀘스트 진행 |
| **5순위** | **던전 내부 전투** | 줍기손 / 초입 3회 탭 / 5초 홀드 | 화면 헛클릭 일체 차단 |
| **6순위** | **던전 선택 화면** | **반짝이는 맵 카드 클릭** -> [입장] 클릭 | **마을 퀘스트보다 최우선 선택** |
| **7순위** | **마을 우상단 [에픽] 퀘스트** | `(x=86.5%, y=18.3%)` 탭 | 마을 길찾기 이동 (3.5초 쿨다운) |
| **8순위** | **가이드 손가락/링 (👆)** | `(tipX, tipY)` 탭 | 튜토리얼 돌파 |

---

## 10. 던전 진입 후 공격하지 않고 멍때리며 맞고만 있던 버그
* **증상**: 던전에 들어왔는데 공격을 전혀 하지 않고 처맞고만 있으며, 바닥에 엉뚱한 터치가 튀는 문제.
* **원인 (실수)**:
  - 1) `checkDungeonSelectScreenInBitmap`에서 좌상단 흰색 픽셀 판정(`titlePts > 150`)을 사용했는데, 실제 던전 전투 중 좌상단에 표시되는 캐릭터 닉네임/레벨/계급 흰색 텍스트(1340픽셀)가 이를 만족해 **전투 중에도 던전 선택창으로 오탐**됨.
  - 2) 이로 인해 `isInDungeonState = false`가 되어 `executionJob`의 공격 꾹 누르기(5초 홀드)가 완전히 정지됨.
  - 3) 또한 던전 선택창으로 오탐된 상태에서 `findQuestMapCardInBitmap`이 실행되어, 전투 중 발생하는 몬스터 피격/스킬 이펙트 점멸을 맵 카드로 착각하고 바닥을 난타함.
  - 4) `startSlotExecution` 시작 시 `isInDungeonState` 초기값이 `false`로 되어 있어 5번을 눌렀을 때 즉시 홀드로 진입하지 못함.
* **해결 조치**:
  - 1) **전투 컨트롤 판정기(`checkCombatControlsInBitmap`) 신설**: 화면에 황금색 검 공격 버튼(`atkPts > 80`) 또는 조이스틱 링(`joyPts > 60`)이 감지되면 **무조건 던전 전투 중(`isCombat = true`)**으로 즉시 판정.
  - 2) 던전 선택창 판정에서 `checkCombatControlsInBitmap` 감지 시 즉시 `false` 반환하여 전투 중 던전 선택창 오탐 원천 차단.
  - 3) `startSlotExecution` 시작 시 `isInDungeonState` 초기값을 `true`로 설정하여 사용자가 5번 버튼을 누르자마자 즉각 5초 공격 홀드 시작.
  - 4) `findBattleStartButtonInBitmap` 및 `findQuestMapCardInBitmap`에도 전투 컨트롤 감지 시 즉시 차단하는 이중 안전장치 적용.

---

## 11. 던전 진입 5회 클릭 및 전투 중 연속 꾹 누르기(홀드) 구조 정립
* **증상**: 던전 첫 진입 시에만 5회 클릭 후 공격 홀드로 넘어가야 하는데, 홀드가 끝날 때마다 임의로 추가했던 스킬 시전 및 조이스틱 스와이프가 튀어나와 전투 흐름이 끊기는 문제.
* **원인 (실수)**:
  - 던전 전투 중 매 홀드마다 임의로 스킬 클릭과 조이스틱 방향 스와이프(`swipeSingle`)를 호출하도록 작성되어 있어, 사용자가 원하는 "던전 초입에만 5회 클릭 후, 그 뒤로는 텀을 두고 꾹 누르기(5초 홀드 공격)만 이어서 계속 반복"하는 단순하고 확실한 동작을 방해함.
* **해결 조치**:
  - 1) `hasDoneDungeonInitialClicks` 플래그 도입: 던전 처음 진입 시 딱 1회만 타겟 공격 버튼 5회 클릭 실행.
  - 2) 던전 내부 전투 루프에서는 임의의 조이스틱 스와이프 및 스킬 난사를 전면 제거하고, **5초 꾹 누르기(Hold) -> 150ms 텀(간격) -> 5초 꾹 누르기(Hold)**를 끊김 없이 연속으로 정직하게 반복하도록 단순화.
  - 3) 던전 클리어/던전 선택/마을 이동 시 `hasDoneDungeonInitialClicks`를 리셋하여 다음 던전 진입 시 정상적으로 5회 클릭 후 홀드하도록 완벽 연동.

---

## 12. 던전 선택 화면에서 지도 맵 카드를 클릭하지 않고 공격 홀드가 돌던 버그
* **증상**: 던전 선택창에 진입했는데 반짝이는 사각형 지도를 클릭하지 않고 공격 버튼 위치를 5초간 꾹 누르고 있는 문제.
* **원인 (실수)**:
  - 던전 전투 감지 조건에 `rawMiniMapDarkRatio > 0.35f`가 포함되어 있었는데, 던전 선택창 화면 우상단 배경 역시 검은색 우주/공백(비율 87%)이라서 던전 선택창을 전투 화면으로 오탐함.
  - 이로 인해 `isCombat = true`, `isDungeonSelect = false`가 되어 던전 선택 화면임에도 맵 카드 클릭 루틴을 건너뛰고 5초 공격 홀드가 실행됨.
* **해결 조치**:
  - `checkDungeonSelectScreenInBitmap`을 먼저 검사하여 던전 선택창일 경우 `isCombat = false`, `inDungeon = false`, `isInDungeonState.set(false)`로 즉각 공격 홀드를 중단.
  - 2) 던전 선택창에서 **반짝이는 퀘스트 맵 카드(`findQuestMapCardInBitmap`)를 최우선 즉시 클릭**하고 활성화된 [입장] 버튼을 클릭하도록 완벽 수정.

---

## 13. 던전 전투 중 우상단 미니맵("상단지도")을 계속 클릭하던 버그
* **증상**: 던전 전투 중에 캐릭터가 공격에 집중하지 않고 우상단 미니맵 위치(`91.5%, 6.0%`)를 1~2초마다 계속 터치하여 지도가 열리거나 홀드가 끊기는 문제.
* **원인 (실수)**:
  - 파이프라인에서 1순위로 배치되어 있던 `checkSkipDialogInBitmap`(대화 건너뛰기 ✕) 검사가 전투 검사보다 먼저 실행됨.
  - 던전 내부 우상단 미니맵의 흰색 테두리/아이콘이 건너뛰기 ✕ 버튼(`x: 86% ~ 97%, y: 3% ~ 9%`)으로 오탐되어, 전투 중임에도 미니맵 위치(`91.5%, 6.0%`)를 계속 클릭함.
* **해결 조치**:
  - **전투 감지(`checkCombatControlsInBitmap`)를 파이프라인 최상위(1순위)로 이동**:
    화면에 공격 버튼 또는 조이스틱이 존재하는 전투 상황에서는 대화 스킵, 팝업, 미니맵 등 **모든 화면 터치를 100% 원천 차단**하고 즉시 `return`.
  - `checkSkipDialogInBitmap` 내부에도 전투 컨트롤 검출 시 즉시 `false`를 반환하도록 이중 방어 적용.

---

## 14. 던전 클리어 메뉴에서 다음 퀘스트를 안 누르고 공격 타겟(화살표 타겟)을 계속 누르던 버그
* **증상**: 보스 처치 후 우측에 던전 클리어 메뉴([마을로 가기], [에픽] 다음 퀘스트)가 떴는데도 퀘스트를 누르지 않고 공격 버튼(화살표 타겟) 위치를 5초간 계속 꾹 누르고 있는 문제.
* **원인 (실수)**:
  - 던전 클리어 메뉴가 화면에 떴을 때도 배경에 공격 버튼 아이콘이 남아 있어 `checkCombatControlsInBitmap`이 `true`로 감지됨.
  - 13번 수정 시 `isCombat`을 최상위에 두었기 때문에 클리어 메뉴 판정(`isDungeonClear`)보다 전투가 우선시되어, 전투가 끝났음에도 공격 홀드가 계속 돌고 클리어 퀘스트 클릭을 건너뜀.
* **해결 조치**:
  - **던전 클리어 메뉴(`isDungeonClear`)를 파이프라인 최상위(1순위)로 승격**:
    클리어 메뉴(파란 마을 + 황금 에픽)가 뜨면 전투 홀드를 즉각 중단(`isInDungeonState.set(false)`)하고 **최우선으로 우상단 다음 에픽 퀘스트(86.5%, 18.3%)를 즉시 클릭**하도록 수정.
  - 2순위에 던전 선택 화면, 3순위에 던전 전투 화면으로 파이프라인 우선순위 최종 정상화.

---

## 15. 던전 내부 전투 중 미니맵 오인식으로 인한 우상단 오클릭 및 방 이동 검은 화면 오작동 버그
* **증상**: 던전 전투 중 우상단에 검은 사각형 미니맵이 떠 있는데도 미니맵 영역을 계속 퀘스트 버튼으로 오인하여 클릭하고, 방 이동 시 검은 화면(페이드아웃)에서 상태가 풀려버리는 문제.
* **원인 (실수)**:
  1. `checkDungeonMiniMapInBitmap` 내부에 `if (checkRetryButtonInBitmap(bitmap)) return false;`가 삽입되어 있었음. 전투 중 몬스터 스킬/이펙트의 파란색/황금색 픽셀이 튀면 `checkRetryButtonInBitmap`이 true로 오탐 -> 미니맵 검출이 false가 됨 -> 던전 클리어 메뉴(`3순위`)로 넘어가 미니맵 내부 좌표(`86.5%, 18.3%`)를 클릭함.
  2. 던전 방 이동 시 화면 전체가 암전(페이드아웃)되는데, 이때 미니맵이 일시적으로 사라지면서 던전 상태가 즉시 해제되어 헛클릭 발생.
* **해결 조치**:
  1. **미니맵 검사에서 retryButton 의존성 완전 제거**: 우상단 어두운 네모 박스(30% 이상 어두운 픽셀)만으로 미니맵 100% 독립 판별.
  2. **`isBlackTransitionScreen` 추가**: 방 이동/로딩 암전 페이드 시 모든 화면 터치 차단 및 던전 상태 래치 유지.
  3. **던전 상태 래치(State Latch) 강화**: 던전 진입 후 미니맵이 사라지고 실제 클리어 결과창 메뉴가 나타나기 전까지는 `DUNGEON_COMBAT` 상태를 유지하여 우상단 헛클릭 원천 차단.

---

## 16. 실시간 상태 HUD 오버레이(Top HUD Overlay) 및 상태 고유번호 매핑 추가
* **요구사항**: 최상단에 클릭을 방해하지 않는(비터치) 검정 배경 + 흰 글자의 HUD를 띄워, 현재 오토클리커가 어떤 상태를 감지하고 어떤 동작을 수행하는지 고유번호와 함께 한글로 실시간 표기.
* **구현 내용**:
  1. **`StatusHudOverlay` 구현**: `FLAG_NOT_TOUCHABLE | FLAG_NOT_FOCUSABLE` 적용으로 터치 입력 100% 관통. 검정 반투명(#E6000000) 배경 + 흰색 굵은 텍스트.
  2. **파이프라인 상태 고유번호 체계 확립**:
     - `[#000]`: 오토클리커 대기 중
     - `[#001]`: 장비 획득 [장착] 팝업 감지 -> 장착 터치
     - `[#100]`: 방 이동/로딩 암전 감지 -> 터치 차단 및 전투 유지
     - `[#110]`: 던전 선택: 깜박이는 퀘스트 카드 감지 -> 맵 카드 터치
     - `[#111]`: 던전 선택: [입장/전투시작] 버튼 감지 -> 입장 터치
     - `[#112]`: 던전 선택 화면 대기 중 (헛클릭 원천 차단)
     - `[#120]`: 던전 전투: 던전 초입 5회 연타 실행 중
     - `[#121]`: 던전 전투: 미니맵 확인됨 -> 5초 꾹 누르기(Hold) 진행 중
     - `[#122]`: 던전 전투: 바닥 아이템 줍기 감지 -> 줍기 터치
     - `[#123]`: 던전 내부: 대화 스킵(✕) 감지 -> 스킵 터치
     - `[#124]`: 던전 전투 래치: 미니맵 일시 미검출 -> 전투 유지 (헛클릭 차단)
     - `[#130]`: 던전 클리어: 결과창 확인 -> 다음 에픽 퀘스트(18.3%) 터치
     - `[#140]`: 대화 스킵: 스토리 대화창 감지 -> 건너뛰기(✕) 터치
     - `[#150]`: 확인 팝업: 중앙 확인 모달 감지 -> 확인 터치
     - `[#160]`: NPC 퀘스트: 최상단 에픽 [보고/수락] 감지 -> 터치
     - `[#170]`: 마을 이동: 우상단 [에픽] 배너 감지 -> 클릭 후 길찾기 이동
     - `[#172]`: 마을 안내: 손가락/노란 원 안내 감지 -> 가이드 터치
     - `[#180]`: 마을 대기: 마을 레이더 감지됨 -> 이벤트 대기 중
     - `[#199]`: 상태 탐색: 화면 분석 및 판별 대기 중

---

## 17. 던전 클리어 결과창(마을로 가기 패널)을 미니맵 전투로 오인식하던 버그 수정
* **증상**: 보스 처치 후 우측에 [마을로 가기], [다시하기], [에픽 퀘스트]가 포함된 결과창 목록 상자가 떴는데도, 오토클리커가 `[#121] 던전 전투: 미니맵 확인됨`으로 오판하여 클리어 버튼을 누르지 않고 공격 홀드를 계속하던 문제.
* **원인 (실수)**:
  1. 클리어 메뉴 패널 자체가 우측 상단을 덮는 짙은 반투명 검은색 배경을 가지고 있어, 우상단 영역(84%~97%, 3%~22%)의 어두운 픽셀 비율이 56.8%로 검출되어 `hasMiniMap = true`가 됨.
  2. `checkAndHandleScreenState`에서 2순위 미니맵 검사가 3순위 클리어 메뉴 검사보다 앞에 위치하여, 클리어 메뉴가 떴음에도 2순위 전투 분기로 빠져버림.
  3. `checkRetryButtonInBitmap` 안에 `if (checkDungeonMiniMapInBitmap(bitmap)) return false`가 있어 클리어 판정이 원천 봉쇄됨.
* **해결 조치**:
  1. **클리어 메뉴 판별식 정밀화**: `checkRetryButtonInBitmap`에서 [마을로 가기] 파란 픽셀(4,200개 이상) + [다시하기]/[에픽 퀘스트] 황금 픽셀(3,000~13,000개) 조합으로 100% 독립 판별. 미니맵 체크 의존성 완전 제거.
  2. **우선순위 승격**: 던전 클리어 메뉴 검사를 미니맵 전투 검사보다 앞선 **2순위**로 배치. 클리어 화면이 뜨면 미니맵 검사로 빠지지 않고 즉시 `[#130]` 상태로 전이되어 다음 에픽 퀘스트(18.3%)를 즉각 클릭하도록 수정.

---

## 18. 대화 씬에서 암전 감지 인터벌 지연 및 미니맵 오인식 버그 수정
* **증상**: 대화 씬에서 건너뛰기(✕)가 화면에 떠 있는데도 `[#100] 방 이동/로딩 암전 감지`로 오판되어 심한 인터벌(딜레이)이 생기고, 미니맵이 없는데도 미니맵 전투(`[#121]`)로 인식되어 공격 홀드가 발동하던 문제.
* **원인 (실수)**:
  1. `isBlackTransitionScreen`(암전 감지)이 최상단에서 먼저 실행되면서, 어두운 컷씬/대화창의 배경을 암전으로 오인하여 화면 터치를 차단하고 대기함.
  2. 대화 씬에서 우상단 영역이 어두운 색상일 때 `checkDungeonMiniMapInBitmap`이 미니맵으로 오판하여 대화 스킵보다 전투 홀드가 우선 실행됨.
* **해결 조치**:
  1. **대화 스킵(건너뛰기 ✕)을 [최우선 1순위]로 전진 배치**: 암전, 미니맵, 클리어보다 최우선으로 즉시 스킵 탭(`[#140]`).
  2. **암전 감지에서 대화창 완전 예외 처리**: 화면에 건너뛰기 ✕ 버튼이 감지되면 절대 암전으로 판정하지 않음 (`if (checkSkipDialogInBitmap) return false`).
  3. **미니맵 판정 강화**: 건너뛰기 ✕가 있거나, 클리어/선택창이거나, 실제 전투 조작계(공격/조이스틱)가 없으면 절대 미니맵으로 인정하지 않도록 차단.

---

## 19. 마을 화면을 던전 전투로 오인식하던 버그 및 좌측 상단 실시간 판단 근거 비전 HUD 추가
* **증상**: 마을에서 우상단에 [에픽] 퀘스트 배너가 뻔히 떠 있는데도 퀘스트를 누르지 않고 `[#121] 던전 전투: 미니맵 확인됨`으로 오판하여 공격 홀드를 돌리던 문제.
* **원인 (실수)**:
  1. 던파 모바일은 마을에서도 캐릭터 조작용 조이스틱과 평타/대시 버튼이 활성화되어 있어 `checkCombatControlsInBitmap`이 true를 반환함.
  2. 마을 우상단 퀘스트 박스의 어두운 반투명 바탕색 때문에 `checkDungeonMiniMapInBitmap`의 어두운 픽셀 비율이 40.9%로 검출되어 미니맵으로 오판됨.
  3. 마을 [에픽] 퀘스트 배너 검사(`7순위`)가 던전 전투 검사(`5순위`)보다 뒤에 있어, 마을 퀘스트 배너가 있어도 미니맵 전투 분기로 먼저 빠져버림.
* **해결 조치**:
  1. **마을 [에픽] 퀘스트 배너를 던전 전투보다 앞선 [4순위]로 승격**: 화면에 황금 에픽 텍스트가 있으면 미니맵 전투로 가지 않고 즉시 에픽 퀘스트(86.5%, 18.3%) 클릭.
  2. **`checkDungeonMiniMapInBitmap`에 마을 에픽 배너 예외 차단**: 마을 에픽 배너가 감지되면 절대 던전 미니맵으로 인정하지 않음 (`return false`).
  3. **좌측 상단 `DebugVisionOverlay` 추가**:
     - 클릭 간섭이 없는 안전 영역(좌측 상단)에 AI가 실시간으로 분석한 **실제 우상단 크롭 영상(ImageView)과 판정 이유/상태명**을 검정 반투명 + 청록색 테두리로 실시간 노출.

---

## 20. 스탑 상태 원샷 화면 진단 'D' 버튼 추가 및 에픽 퀘스트 배너 중심 좌표 조정
* **요구사항**: 스탑 상태에서 사용자가 `D` 버튼을 누르면 현재 화면을 즉시 캡처하고 상태를 진단하여 무엇을 할지 화면과 토스트로 적어서 보여주는 기능 요청 및 에픽 로터스 퀘스트 배너 터치 좌표 보정.
* **구현 내용**:
  1. **플로팅 메뉴에 `D` (진단) 버튼 추가**: `FloatingOverlayManager`에 청록색 `D` 버튼 배치.
  2. **`diagnoseCurrentScreenState()` 구현**:
     - 현재 화면을 즉시 캡처하여 0~9순위 파이프라인 조건을 전부 평가.
     - 판정 결과(상태명, 고유코드 #XXX)와 AI가 실행할 예정 동작(클릭 대상 좌표)을 생성.
     - 우상단 핵심 영역을 크롭하여 좌측 상단 `DebugVisionOverlay`에 띄우고, 최상단 `StatusHudOverlay` 및 안드로이드 긴 토스트로 즉각 표시.
  3. **마을 [에픽] 퀘스트 배너 클릭 좌표 보정**:
     - 기존 배너 상단 테두리(`18.3%`)에서 배너 글자 중심인 `(85.0%, 21.0%)`로 정확히 타격하도록 수정.

---

## 21. 마을 화면 상단 햄버거 메뉴를 '건너뛰기(✕)'로 오판하던 버그 수정 및 팝업 우선순위 정상화
* **증상**: 마을에서 우측 퀘스트 트래커의 최상단 에픽 퀘스트 `[에픽] 로터스의 정신지배`를 클릭해야 할 타이밍에, AI가 `[140] 건너뛰기 클릭 예정`으로 판정하며 우상단 햄버거 메뉴(≡)를 계속 헛클릭하던 문제.
* **원인 (실수)**:
  1. 마을 화면 우상단의 3줄 햄버거 메뉴 아이콘(≡)이 흰색 가로 막대 3개로 구성되어 있어, `checkSkipDialogInBitmap`의 흰색 픽셀 검사(`x: 86% ~ 97%, y: 3% ~ 9%`)에서 2,000픽셀 이상 검출되어 Skip 버튼으로 오인식됨.
  2. 슈시아 주점 등 어두운 바닥 텍스처로 인해 하단 딤드 영역 검사(`darkRatio > 0.30f`)까지 우연히 통과됨.
  3. `isSkipDialog`가 파이프라인 1순위에 위치하여 마을 에픽 퀘스트 배너(`hasEpicQuest`)보다 먼저 실행되어, 에픽 퀘스트 배너가 명확히 보임에도 건너뛰기 분기로 빠짐.
* **해결 조치**:
  1. **마을/에픽 배너 상태 시 건너뛰기 원천 배제**: `checkSkipDialogInBitmap` 최상단에 `checkEpicBannerInBitmap`, `checkQuestAuraInBitmap`, `checkTownScreenInBitmap` 감지 시 즉시 `return false` 처리.
  2. **건너뛰기 배경 색상 검증 추가**: 실제 컷씬 건너뛰기(✕)는 배경이 암색(darkRatio >= 40%)인 반면, 마을 햄버거 메뉴는 갈색 목재 현판(woodRatio > 20%)이므로 목재 배경 감지 시 건너뛰기 판정 차단.
  3. **팝업 우선순위 재정렬**:
     - 0순위: 장비 획득 [장착] 팝업
     - 1순위: 완료/확인/이동 모달 [확인] 팝업
     - 2순위: NPC 퀘스트 최상단 에픽 [보고/수락] 팝업
     - 3순위: 대화 스킵 (건너뛰기 ✕)
     - ...
     - 6순위: 마을 우상단 [에픽] 퀘스트 배너 `(85.0%, 21.0%)`
  4. **스탑 진단(D 버튼) 화면별 맞춤 근거 이미지 크롭**: 각 판정 상태(장착, 확인, 퀘스트수락, 건너뛰기, 에픽배너 등)에 맞는 실제 화면 영역을 좌측 상단 비전 HUD에 정확히 표시하도록 개선.

---

## 22. 최근 10분 동작 블랙박스 자동 보관(스크롤/페이징 뷰어) 구현
* **요구사항**: 최근 10분간의 동작(시간, 고유코드 #XXX, 판정 상태명, 타겟 클릭 좌표 및 캡처 근거 이미지)을 로컬에 자동 보관하고, 스크롤바와 페이징으로 직관적으로 확인할 수 있는 뷰어 요청.
* **구현 내용**:
  1. **`ActionHistoryManager` (10분 롤링 버퍼 및 로컬 저장소)**:
     - 모든 자동 동작(장비 장착, 확인 모달, 퀘스트 수락, 대화 스킵, 클리어, 던전 선택, 에픽 배너, 가이드 손가락, 전투) 및 'D' 수동 진단 시 캡처된 비전 근거 이미지를 JPEG로 로컬 디렉터리(`filesDir/action_history/`)에 비동기 저장.
     - 10분(`10 * 60 * 1000ms`)이 경과한 오래된 레코드 및 이미지 파일을 실시간으로 자동 정리(Pruning)하여 디스크 용량 낭비 방지.
  2. **`ActionHistoryOverlay` (스크롤 및 페이징 플로팅 뷰어)**:
     - 플로팅 패널의 보라색 `[📋]` 버튼 터치 시 게임 화면 위에 즉각 오버레이 윈도우 표시.
     - **페이징 및 스크롤 지원**: 10건 단위 페이징(`[◀] 1 / N [▶]`), 부드러운 수직 스크롤바(`isVerticalScrollBarEnabled = true`), 최신 동작 역순 정렬.
     - **카드별 세부 정보 제공**: 시각(`[HH:mm:ss.SSS]`), 고유코드 배지(`[#XXX]`), 상태 타이틀, 세부 클릭 좌표 설명, 캡처된 실제 비전 썸네일 이미지 동시 표시.
  3. **빌드 및 배포 완료**:
     - `FloatingOverlayManager`에 `[📋]` 버튼 배치, 서비스 생명주기 및 메모리 누수 방지 처리 완료.

---

## 23. 던전 내부 전투(미니맵)를 마을 에픽 퀘스트 배너로 오판하여 헛클릭하던 버그 수정 및 미니맵 우선권 확립
* **증상**: 던전(제2척추 등) 내부에서 전투를 수행해야 하는 상황임에도, AI가 우상단 미니맵 아래의 던전 이름("제2척추") 황금색 텍스트를 `[#170] 마을 이동: 우상단 [에픽] 배너 감지`로 오인식하여, 전투를 하지 않고 미니맵 영역 `(85.0%, 21.0%)`을 무한 연타하던 문제.
* **원인 (실수)**:
  1. **황금색 픽셀 임계값 부족**: `checkEpicBannerInBitmap`의 황금색 판정 기준이 고작 `> 60` 픽셀로 너무 낮아, 던전 이름 텍스트("제2척추" 약 746픽셀)에 의해 쉽게 참으로 판정됨.
  2. **미니맵 검사 자기 검열**: `checkDungeonMiniMapInBitmap` 내부에 `if (checkEpicBannerInBitmap(bitmap)) return false`가 걸려 있어, 던전 이름으로 인해 에픽 배너가 참이 되자 미니맵 검사가 false로 튕겨 나감.
  3. **파이프라인 우선순위 역전**: 마을 에픽 퀘스트 배너(`6순위`)가 던전 전투(`9순위`)보다 앞서 평가되어, 던전 안에서도 에픽 배너 조건이 먼저 걸리면 전투를 못 하고 퀘스트 클릭으로 빠짐.
* **해결 조치**:
  1. **던전 전투(`hasMiniMap`)를 에픽 배너보다 앞선 [6순위]로 전진 배치**:
     - 0순위: 장비 장착 팝업
     - 1순위: 확인/완료 모달
     - 2순위: NPC 퀘스트 보고/수락
     - 3순위: 대화 스킵
     - 4순위: 던전 클리어 메뉴
     - 5순위: 던전 선택 화면
     - **6순위: 던전 내부 실제 전투 (`hasMiniMap`) -> 공격 홀드 + 던전 래치 보호**
     - 7순위: 방 이동 로딩/암전 (`isBlackTransitionScreen`)
     - **8순위: 마을 우상단 에픽 퀘스트 배너 (`hasEpicQuest`)**
  2. **`checkDungeonMiniMapInBitmap` 인식 알고리즘 강화**:
     - `checkEpicBannerInBitmap` 호출 제거 (상호 잠식 차단).
     - 던전 미니맵 박스 영역(`x: 82%~97%, y: 3%~20%`)의 어두운 배경 비율(`darkRatio >= 0.35f`) 검증.
     - 마을 우상단 목재 현판 픽셀 점수(`woodPts <= 2000`) 검증으로 마을 화면 완벽 배제.
  3. **`checkEpicBannerInBitmap` 오인식 방지**:
     - `if (checkDungeonMiniMapInBitmap(bitmap)) return false`를 선두에 두어 던전 미니맵이 있으면 에픽 배너 판정을 원천 차단.
     - 황금색 픽셀 임계값을 기존 `> 60`에서 실제 3줄 텍스트 크기인 `> 400` 이상으로 대폭 상향.
  4. **스탑 진단(D 버튼) 평가 순서 동기화**: `diagnoseCurrentScreenState`에서도 던전 전투가 에픽 퀘스트보다 먼저 평가되도록 수정.

---

## 24. 미니맵/스킵/에픽배너 간 상호 재귀 호출로 인한 StackOverflowError 크래시 및 접근성 권한 해제 수정
* **증상**: 앱이 즉시 크래시(Crash)나서 뻗어버리고, 이미 활성화되어 있던 접근성(Accessibility) 권한이 안드로이드 OS에 의해 강제 비활성화되어 앱을 켤 때마다 다시 접근성 권한을 켜라고 요구하던 문제.
* **원인 (치명적 실수)**:
  - **상호 무한 순환 재귀 (Circular Mutual Recursion)** 발생:
    1. `checkEpicBannerInBitmap`이 `checkDungeonMiniMapInBitmap`을 호출.
    2. `checkDungeonMiniMapInBitmap`이 `checkSkipDialogInBitmap`을 호출.
    3. `checkSkipDialogInBitmap`이 `checkEpicBannerInBitmap`을 호출.
    - 위 3개 함수가 꼬리를 물고 무한 호출되어 JVM 콜스택이 폭발, `java.lang.StackOverflowError`가 발생하여 접근성 서비스 프로세스가 즉사함.
    - 안드로이드 OS의 `AccessibilityManagerService`는 접근성 서비스 프로세스가 Uncaught Exception으로 비정상 종료되면 시스템 보호를 위해 해당 서비스를 시스템에서 자동 해제/언바인드 처리함.
* **해결 조치**:
  1. **완전한 비순환 단방향 구조(Leaf & Level-1 DAG)로 전면 개편**:
     - `checkEpicBannerInBitmap`: `checkDungeonMiniMapInBitmap` 호출을 전면 삭제. 대신 자체적으로 마을 목재 현판 픽셀(`woodPts > 2000`) 및 `checkTownScreenInBitmap`만 직접 검사하여 순환 원천 차단.
     - `checkSkipDialogInBitmap`: `checkEpicBannerInBitmap` 및 `checkQuestAuraInBitmap` 호출 전면 삭제. 자체적인 하단 대화창 딤드 영역(`darkRatio >= 0.40f`) 및 햄버거 메뉴 목재 배제 로직으로 판정.
     - `checkDungeonMiniMapInBitmap`: `checkSkipDialogInBitmap`, `checkRetryButtonInBitmap`, `checkDungeonSelectScreenInBitmap` 호출 전면 삭제. 자체적인 미니맵 다크 박스 및 전투 컨트롤 존재 여부로만 독립 판정.
     - `isBlackTransitionScreen`: `checkSkipDialogInBitmap` 호출 제거하여 순수 암전 픽셀 검사로 단순화.
  2. **시스템 접근성 서비스 재등록 및 자동 바인딩 복원**:
     - ADB 명령어로 `Settings.Secure.ENABLED_ACCESSIBILITY_SERVICES`에 `AutoClickAccessibilityService`를 안전하게 갱신하고 재연결 완료 (`AccessibilityService connected`).

---

## 25. 우상단 사각형 미니맵 형태 판정 강화 및 에픽 퀘스트 10초 이동 대기(더블클릭/가다서다 방지)
* **증상**:
  1. 마을 화면에서 퀘스트 배너를 누른 뒤 캐릭터가 NPC로 걸어가는 동안 2.5초마다 퀘스트 배너를 재클릭하여 캐릭터가 가다 서다(멈칫)를 반복하던 문제.
  2. 오토클리커 정지(⏹) 시 이전에 검출되었던 던전 미니맵 크롭 화면이 비전 HUD에 잔상으로 남아있어 현재 화면을 던전 미니맵으로 착각하게 만들던 문제.
* **원인**:
  1. 길찾기 이동 중 보호 시간이 2.5초로 너무 짧아, 이동 시간이 긴 퀘스트에서 캐릭터가 걷는 도중 퀘스트 배너를 계속 헛클릭함.
  2. 미니맵 검사 시 우상단 어두운 배경 비율만 단순 체크하여 주점 등 어두운 마을 배경과 명확한 구분이 약했음.
* **해결 조치**:
  1. **우상단 사각형 미니맵 형태(Square Minimap) 다중 검증 로직 적용**:
     - 영역: `x: 86% ~ 97%, y: 4% ~ 20%`
     - 플레이어 위치 핀 검출: 파란색 콘 마커(`bluePin >= 30` 또는 `redArrow >= 20`)
     - 던전 방(Room) 사각 격자 타일: 황토/갈색 타일(`roomTiles >= 2000`)
     - 미니맵 하단 던전 이름 텍스트: `yellowName >= 100` ("제2척추" 등)
     - `(플레이어 마커 OR 방 격자 구조) && (던전명 OR 어두운 박스 30% 이상)` 일 때만 미니맵으로 판정하여 마을 화면 100% 배제.
  2. **에픽 퀘스트 배너 10초 이동 보호 락 & 더블 클릭 적용**:
     - 에픽 퀘스트 배너 터치 시 1회 클릭 대신 **더블 클릭(`tapDouble`)** 실행.
     - 이후 **10초간(`10,000ms`)** 에픽 퀘스트 재클릭을 완전히 차단하고 "🚶 [마을 길찾기 이동 중] 목적지로 이동 중 (N초 대기)" 상태로 캐릭터가 자연스럽게 목적지까지 걷도록 보장.
     - 10초가 지난 후에도 여전히 에픽 퀘스트 배너가 남아있으면 그때 다시 더블클릭 실행.
  3. **정지(⏹) 시 비전 HUD 잔상 제거**:
     - `stopClicking()` 호출 시 `DebugVisionOverlay`의 비전 이미지 및 상태 텍스트를 즉시 null로 초기화하여 이전 잔상이 남지 않도록 개선.

---

## 26. 대화 건너뛰기 1초 5회 초고속 연타 적용 및 던전 '약간 시커먼 네모'(미니맵) 최우선 격상 (클리어 오판 해결)
* **증상**:
  1. 던전("달빛주점" 시나리오 등) 내부에서 우상단에 약간 시커먼 네모(미니맵)가 분명히 떠 있는데도, 전투를 하지 않고 `[#130] 던전 클리어 감지`로 오판하여 우상단(18.3%)을 헛클릭하던 문제.
  2. 컷씬/대화 발생 시 '건너뛰기(✕)' 버튼을 1회만 클릭하여 대화 스킵이 느리고 답답하던 문제.
* **원인**:
  1. **던전 클리어(`4순위`)가 던전 전투(`6순위`)보다 앞서 평가**되었고, 던전 내부의 파란색 "NEW GO! ▶" 길안내 화살표와 우하단 황금 공격 버튼 픽셀이 `checkRetryButtonInBitmap`([마을로 가기] + [다시하기]) 조건에 우연히 맞아떨어져 던전 클리어 메뉴로 오판됨.
  2. 시나리오 던전(달빛주점 등)은 방이 1~2개뿐이라 방 타일 점수가 낮아 기존 미니맵 검사에서 누락될 위험이 있었음.
* **해결 조치**:
  1. **건너뛰기 1초 5회 초고속 연타(`tapRapid`) 적용**:
     - 3순위 `isSkipDialog` 감지 시 1회 클릭 대신 60ms 간격으로 **초고속 5연타(`tapRapid(count=5)`)** 실행하여 0.5초 이내에 컷씬/대화창을 즉각 스킵 완료.
  2. **우상단 '약간 시커먼 네모'(미니맵) 감지 강화**:
     - 영역: `x: 86% ~ 97%, y: 3.5% ~ 25%`
     - 반투명 어두운 사각 배경(`darkRatio >= 0.30f`) + 마을 목재 현판 배제(`woodPts <= 1800`).
     - 플레이어 위치 핀(파란 마커 `bluePin >= 25` 또는 빨간 화살표 `redArrow >= 25`) 또는 던전명(달빛주점/제2척추 `yellowName >= 80`) 검출로 100% 확정.
  3. **던전 전투(`hasMiniMap`)를 던전 클리어보다 앞선 [4순위]로 격상**:
     - 0순위: 장비 장착 팝업
     - 1순위: 확인/완료 모달
     - 2순위: NPC 퀘스트 수락
     - 3순위: **대화 스킵 (1초 5회 초고속 연타)**
     - **4순위: 던전 전투 (우상단 약간 시커먼 네모 미니맵) -> 공격 홀드 및 던전 래치 보호**
     - 5순위: 던전 클리어 메뉴 (미니맵이 없을 때만 실행)
     - 6순위: 던전 선택 화면
     - 7순위: 방 이동 로딩/암전
     - 8순위: 마을 에픽 퀘스트 배너 (10초 대기 & 더블 클릭)
     - 우상단에 시커먼 미니맵 네모가 떠 있는 동안에는 절대 클리어 메뉴나 마을 퀘스트로 빠지지 않고 100% 전투만 수행.

---

## 27. 판별 파이프라인 서열 전면 개편 (던전 래치 트랩 해소 및 다중 위치 장착 팝업 지원)
* **증상**:
  1. 보스 처치 후 던전 클리어 메뉴([마을로 가기], [에픽: 라라아와 대화] 등)가 떴는데도, 오토클리커가 허공에 평타를 치며 빠져나오지 못하고 무한 루프에 갇히던 문제.
  2. 던전 클리어 시 우하단(`x: 88.5%, y: 85.0%`)에 나타난 주황색 [장착] 버튼이 감지되지 않던 문제.
* **원인**:
  1. **던전 래치(Latch) 트랩**: 던전 진입 플래그(`isInDungeonState`)와 조작키 잔존 조건이 던전 클리어(`5순위`) 및 던전 선택(`6순위`)보다 앞서 평가되어, 미니맵이 완전히 사라졌음에도 배경의 공격 버튼 때문에 전투 상태로 오인되어 클리어 메뉴를 누르지 못함.
  2. **폰트 간섭 오인**: `findEquipButtonInBitmap`이 단일 픽셀만 샘플링하여, 주황색 버튼 중앙에 적힌 흰색 폰트 "장착" 글자(RGB: 255, 255, 255)에 정확히 맞아떨어져 주황색 검출이 실패함.
* **해결 조치**:
  1. **파이프라인 평가 서열(Pipeline Hierarchy) 전면 재배치**:
     - **0순위**: 장비 획득 [장착] 팝업 (y: 68% 및 y: 85% 다중 영역 스캔)
     - **1순위**: 확인/완료 중앙 모달 (`56.0%, 63.0%`)
     - **2순위**: NPC 퀘스트 보고/수락 (`65.5%, 35.5%`)
     - **3순위**: 대화 스킵 (1초 5회 초고속 연타 `tapRapid`)
     - **4순위**: 던전 실제 전투 (우상단 '약간 시커먼 네모' 미니맵 실제 존재 시에만 공격 홀드)
     - **5순위**: **던전 클리어 메뉴** (최상단 다음 퀘스트 `16.0%` 터치 + `isInDungeonState` 해제)
     - **6순위**: **던전 선택 화면** (퀘스트 맵 카드/입장 터치 + `isInDungeonState` 해제)
     - **7순위**: 방 이동 로딩/암전 (`isBlackTransitionScreen`)
     - **8순위**: **던전 래치 보호 (`isInDungeonState && !isTown`)** (클리어/던전선택/마을 레이더를 배제한 순수 스킬 이펙트 가림 상황에서만 안전하게 동작)
     - **9순위**: 마을 에픽 퀘스트 배너 (더블 클릭 후 10초 길찾기 이동 보호 락)
     - **10순위**: 가이드 손가락 / 노란 원형 링 안내
  2. **장착 버튼 다중 위치 및 면적 스캔(`orangeCount > 30`) 적용**:
     - 위치 1: 일반/레벨업 보상 (`x: 90.5%, y: 68.0%`)
     - 위치 2: 던전 완료 보상 (`x: 88.5%, y: 85.0%`)
     - 단일 픽셀 검사 대신 버튼 영역 전체의 주황색(`orangeCount > 30`) 픽셀 집합 스캔으로 흰색 텍스트 간섭을 원천 무력화.
## 28. 순위(워터폴) 구조 폐기 및 순수 상태 패턴(State Machine) 전환 & 던전 미니맵 판별 정상화
* **증상**:
  1. 던전 전투 중임에도 우상단 상태바/일시정지 버튼의 파란 픽셀 오인으로 오토클리커가 던전을 마을(`TOWN`)로 착각하여 공격을 멈추고 마을 퀘스트를 기다리는 치명적 결함 발생.
  2. 순위 기반(워터폴) 로직으로 인해 매 프레임마다 모든 상태가 전역 평가되어, 던전 전투 중에 마을 길찾기가 간섭하거나 반대로 마을에서 전투 키가 오동작하는 혼선 발생.
* **원인**:
  1. `checkDungeonMiniMapInBitmap` 함수 서두에 `if (checkTownScreenInBitmap(bitmap)) return false`가 걸려 있었음.
  2. `checkTownScreenInBitmap`이 우상단 `(x: 95%~98%, y: 2%~7%)` 영역에서 파란 픽셀이 2%만 넘어도 무조건 마을로 판정하는 오탐(False Positive)을 일으켜, 던전 미니맵 검출이 시작도 못 하고 차단됨.
  3. 전역 순위(0~10순위) 구조로 인해 게임의 현재 맥락(Context)이 유지되지 못하고 화면 노이즈 하나에 상태가 뒤흔들림.
* **해결 조치**:
  1. **워터폴 서열 완전 폐기 및 순수 상태 패턴(State Machine / FSM) 전면 도입**:
     - `GrowthState` 상태 정의: `TOWN`, `TOWN_WALKING`, `DUNGEON_SELECT`, `DUNGEON_LOADING`, `DUNGEON_COMBAT`, `DUNGEON_CLEAR`.
     - 각 프레임마다 현재 상태(`when (growthState)`) 내에서만 유효한 전이 조건과 행동만 실행되도록 캡슐화.
     - 던전 전투 공격 홀드는 오직 `DUNGEON_COMBAT` 상태에서만 동작하며, 마을/길찾기/클리어 중에는 공격 홀드가 100% 원천 차단됨.
  2. **우상단 정사각 '약간 시커먼 네모' 던전 미니맵 식별 로직 정립**:
     - `checkDungeonMiniMapInBitmap`에서 `checkTownScreenInBitmap` 의존성 완전 제거.
     - 미니맵의 어두운 반투명 사각 영역(`x: 86%~97%, y: 3.5%~25%`, `darkRatio >= 0.28`), 플레이어 핀/화살표/던전명 황금 폰트, 마을 목재 현판 부재(`woodPts <= 2000`), 실제 전투 컨트롤 존재를 종합 검증.
  3. **마을 판별 정밀도 극대화**:
     - 던전 미니맵이 존재하면 절대 마을로 판정하지 않음(`if (checkDungeonMiniMapInBitmap(bitmap)) return false`).
     - 마을 고유의 대형 목재 현판(`woodPts > 2200`) 또는 검증된 레이더 구체(`ratio > 0.08 && woodPts > 1500`)가 있을 때만 마을로 확정.
  4. **프레임 단위 상태 자동 동기화(State Auto-Sync)**:
 
## 29. 던전 [입장/전투시작] 버튼 vs [장착] 팝업(#001) 오인 충돌 해소 및 안티스턱 방어 강화
* **증상**:
  1. 던전 선택 화면에서 황금색 [입장 / 전투시작] 버튼(`x: 83.6%, y: 85.2%`)을 우하단 장비 획득 [장착] 팝업(`x: 88.5%, y: 85.0%`, #001)으로 오인하여, 전투 시작을 누르지 못하고 #001 팝업으로 무한 루프 도는 현상 발생.
  2. [장착] 팝업 감지 시 터치 후에도 동일 좌표가 계속 잡히면 다음 상태(전투시작/맵선택 등)로 빠져나가지 못하고 해당 위치에 갇히는 문제.
  3. `checkRetryButtonInBitmap`과 `checkDungeonSelectScreenInBitmap` 간 상호 재귀 호출로 인한 `StackOverflowError` 발생.
* **원인**:
  1. **입장 버튼 황금 비율 임계값 과다**: `findBattleStartButtonInBitmap`의 황금색 비율 조건이 `ratio > 0.08f` (8%)로 설정되어 있었으나, 3120x1440 해상도에서 실제 [입장] 버튼의 황금 픽셀 비율은 약 5.06%(1,645px / 32,500px)에 불과하여 `checkBattleStartInBitmap`이 `false`를 반환함.
  2. **장착 팝업 위치 2번의 영역 침범**: `checkBattleStartInBitmap`이 실패하자, `findEquipButtonInBitmap`의 위치 2번(`y: 85%`)이 던전 선택창 우측 하단의 [입장] 버튼 주황 픽셀(413px)을 던전 클리어 장착 팝업으로 오인하여 `#001`로 반환함.
  3. **상호 재귀(Mutual Recursion)**: `checkRetryButtonInBitmap` 내부에서 `checkDungeonSelectScreenInBitmap`을 호출하고, `checkDungeonSelectScreenInBitmap` 내부에서 `checkRetryButtonInBitmap`을 상호 호출하여 스택 오버플로우 유발.
* **해결 조치**:
  1. **[입장/전투시작] 버튼 판별 기준 현실화**:
     - `findBattleStartButtonInBitmap`의 임계값을 `ratio > 0.03f && goldPixels > 300`으로 조정하여 실기기(3120x1440)의 입장 버튼(`83.6%, 85.2%`)을 100% 정밀 식별하도록 수정.
  2. **장착 팝업 위치 2번 격리 및 상호 배제(Mutual Exclusion)**:
     - `findEquipButtonInBitmap`에 `if (checkBattleStartInBitmap(bitmap)) return null` 및 `if (checkDungeonMiniMapInBitmap(bitmap)) return null` 차단 가드 적용.
     - 위치 2번(`y: 85%`)은 오직 던전 클리어 결과 화면(`checkRetryButtonInBitmap`)에서만 활성화되도록 엄격히 격리.
  3. **상호 재귀 완전 제거**:
     - `checkRetryButtonInBitmap`에서 불필요한 `checkDungeonSelectScreenInBitmap` 호출을 제거하여 단독 판별 보장 및 스택 오버플로우 원천 차단.
  4. **#001 장착 팝업 안티스턱(Anti-Stuck) 폴백 도입**:
     - `consecutiveEquipClicks` 카운터를 도입하여, [장착] 버튼이 2회 연속 터치된 후에도 동일 판정이 지속될 경우 즉시 해당 인터럽트를 통과시켜 상태 머신(던전 선택 / 전투시작 등)이 지연 없이 바로 실행되도록 방어 로직 구현.
  5. **진단(D) 툴 안정성 강화**:
     - `diagnoseCurrentScreenState`에서 `resources.displayMetrics` 대신 캡처된 `bitmap.width`, `bitmap.height`를 직접 참조하고, `try-catch`로 예외를 완벽 보호.
